import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import '@fortawesome/fontawesome-free/css/all.css'; // Ensure you are using the correct path to the FontAwesome CSS file


createApp(App).mount('#app')
